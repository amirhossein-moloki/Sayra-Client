<UserControl x:Class="Sayra.UI.Controls.AdCarousel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="MainContainer" Background="#111216">
        <Grid.Resources>
            <!-- Fade animation -->
            <Storyboard x:Key="FadeInStoryboard">
                <DoubleAnimation Storyboard.TargetName="MainContainer"
                                 Storyboard.TargetProperty="Opacity"
                                 From="0.0" To="1.0" Duration="0:0:0.5"/>
            </Storyboard>
            <Storyboard x:Key="FadeOutStoryboard">
                <DoubleAnimation Storyboard.TargetName="MainContainer"
                                 Storyboard.TargetProperty="Opacity"
                                 From="1.0" To="0.0" Duration="0:0:0.3"/>
            </Storyboard>
        </Grid.Resources>

        <!-- Media content displays -->
        <Image x:Name="AdImage" Stretch="UniformToFill" Visibility="Collapsed" />

        <MediaElement x:Name="AdVideo" Stretch="UniformToFill" LoadedBehavior="Manual" UnloadedBehavior="Manual" Visibility="Collapsed" Volume="0.5" />

        <WebBrowser x:Name="AdWeb" Visibility="Collapsed" />

        <!-- Control overlays (optional, responsive branding) -->
        <Border VerticalAlignment="Bottom" HorizontalAlignment="Right" Background="#CC000000" CornerRadius="4,0,0,0" Padding="12,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="CampaignNameText" Text="Advertisement" Foreground="#FFCC00" FontSize="12" FontWeight="Bold" FontFamily="Segoe UI" Margin="0,0,10,0"/>
                <TextBlock x:Name="TimerText" Text="10s" Foreground="White" FontSize="12" FontFamily="Segoe UI"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
