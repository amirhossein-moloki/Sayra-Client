<UserControl x:Class="Sayra.UI.Controls.SystemInfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border Background="#1AFFFFFF" CornerRadius="12" Padding="15" Margin="0,0,0,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Width="40" Height="40" Background="#1AF5FF00" CornerRadius="8" Margin="0,0,15,0">
                <Path Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-6h2v6zm0-8h-2V7h2v2z"
                      Fill="{StaticResource AccentYellowBrush}" Width="20" Height="20" Stretch="Uniform"/>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Label}" Style="{StaticResource SecondaryText}" FontSize="12"/>
                <TextBlock Text="{Binding Value}" Style="{StaticResource HeadingText}" FontSize="16" Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
