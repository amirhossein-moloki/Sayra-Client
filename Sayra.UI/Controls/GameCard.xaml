<UserControl x:Class="Sayra.UI.Controls.GameCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border Width="150" Height="220" Background="{StaticResource CardBrush}" CornerRadius="12"
            BorderBrush="#33FFFFFF" BorderThickness="1" Margin="10" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform x:Name="CardScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleX" To="1.05" Duration="0:0:0.2"/>
                        <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleY" To="1.05" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleX" To="1.0" Duration="0:0:0.2"/>
                        <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleY" To="1.0" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border CornerRadius="12,12,0,0" Background="#1FFFFFFF" Margin="2">
                <TextBlock Text="IMAGE" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Style="{StaticResource SecondaryText}" Opacity="0.3"/>
            </Border>

            <Border Grid.Row="1" Padding="12">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" Style="{StaticResource HeadingText}" FontSize="14" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Category}" Style="{StaticResource SecondaryText}" FontSize="11" Margin="0,2,0,10"/>
                    <Button Content="PLAY" Style="{StaticResource PlayButton}" Height="30" FontSize="12"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
