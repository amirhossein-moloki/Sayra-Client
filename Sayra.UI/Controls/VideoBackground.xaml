<UserControl x:Class="Sayra.UI.Controls.VideoBackground"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Full-screen MediaElement -->
        <MediaElement x:Name="BackgroundVideo"
                      Stretch="UniformToFill"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Manual"
                      IsMuted="True"
                      MediaOpened="BackgroundVideo_MediaOpened"
                      MediaEnded="BackgroundVideo_MediaEnded"
                      MediaFailed="BackgroundVideo_MediaFailed" />

        <!-- Cinematic dark overlay -->
        <Border x:Name="OverlayBorder" Background="{Binding OverlayBackground, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </Grid>
</UserControl>
