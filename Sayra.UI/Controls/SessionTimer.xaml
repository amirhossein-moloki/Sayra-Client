<UserControl x:Class="Sayra.UI.Controls.SessionTimer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <TextBlock x:Name="TimerText" Text="{Binding SessionTime}" FontFamily="{StaticResource PeydaBlack}" FontSize="90"
               Foreground="White" VerticalAlignment="Center">
        <TextBlock.Effect>
            <DropShadowEffect BlurRadius="30" Color="Black" Opacity="0.5" ShadowDepth="0"/>
        </TextBlock.Effect>
        <TextBlock.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <DoubleAnimation Storyboard.TargetName="TimerText" Storyboard.TargetProperty="Opacity"
                                         From="1.0" To="0.8" Duration="0:0:1" AutoReverse="True"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </TextBlock.Triggers>
    </TextBlock>
</UserControl>
