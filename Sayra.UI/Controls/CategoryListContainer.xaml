<UserControl x:Class="Sayra.UI.Controls.CategoryListContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             Width="228"
             MinWidth="228"
             MinHeight="420"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Custom Category List Scrollbar Thumb Style -->
        <Style x:Key="CategoryListScrollThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle"
                                CornerRadius="8"
                                Background="{DynamicResource Theme.Brushes.Navigation.Background}"
                                SnapsToDevicePixels="True" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="rectangle" Property="Background" Value="{DynamicResource Theme.Brushes.Navigation.ItemActive}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Category List Scrollbar Style -->
        <Style x:Key="CategoryListScrollbarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" HorizontalAlignment="Center">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" Opacity="0" Focusable="false" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" Opacity="0" Focusable="false" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource CategoryListScrollThumbStyle}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Category List ScrollViewer Style -->
        <Style x:Key="CategoryListScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}"/>
                            <ScrollBar Grid.Column="1" x:Name="PART_VerticalScrollBar"
                                       Style="{StaticResource CategoryListScrollbarStyle}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Margin="8,0,0,0" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- ScrollViewer for Vertical Scroll Only, reserving Scrollbar space -->
        <ScrollViewer Style="{StaticResource CategoryListScrollViewerStyle}"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent">
            <!-- Border wraps StackPanel to provide Padding since StackPanel lacks it -->
            <Border Padding="0,4,0,4">
                <!-- Vertical Stack Layout -->
                <StackPanel Orientation="Vertical">
                    <!-- Spacing between placeholders: 6px -->
                    <!-- Placeholder 1 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 2 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 3 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 4 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 5 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 6 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 7 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 8 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 9 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 10 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 11 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 12 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 13 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 14 -->
                    <Grid Height="42" Margin="0,0,0,6">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                    <!-- Placeholder 15 -->
                    <Grid Height="42" Margin="0,0,0,0">
                        <Rectangle Stroke="{DynamicResource Theme.Brushes.Navigation.ItemActive}" StrokeThickness="1" StrokeDashArray="4,4" RadiusX="8" RadiusY="8" Fill="Transparent" />
                    </Grid>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
