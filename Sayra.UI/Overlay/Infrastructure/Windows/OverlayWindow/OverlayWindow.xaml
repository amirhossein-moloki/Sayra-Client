<Window x:Class="Sayra.UI.Overlay.Infrastructure.Windows.OverlayWindow.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SAYRA Game Overlay"
        Height="100" Width="260"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Grid>
        <!-- Outer container with rounded corners and semi-transparent background -->
        <Border x:Name="OverlayBorder" CornerRadius="12" Background="#DD1F1F23" BorderBrush="#FF3E3E42" BorderThickness="1.5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.5" ShadowDepth="2"/>
            </Border.Effect>
            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left icon indicator -->
                <Viewbox Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Path x:Name="StateIcon" Fill="#FFFF3D" Data="M12,2C6.48,2,2,6.48,2,12s4.48,10,10,10,10-4.48,10-10S17.52,2,12,2z M13,17h-2v-6h2V17z M13,9h-2V7h2V9z"/>
                </Viewbox>

                <!-- Right content text -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="TimeTextBlock" Text="00:00:00" Foreground="#FFFFFFFF" FontSize="20" FontWeight="Bold" FontFamily="Segoe UI" />
                    <TextBlock x:Name="StatusTextBlock" Text="Active Session" Foreground="#FFAAAAAA" FontSize="12" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" FontFamily="Segoe UI" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
