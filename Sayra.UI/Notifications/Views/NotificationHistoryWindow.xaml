<Window x:Class="Sayra.UI.Notifications.Views.NotificationHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Sayra.UI.Notifications.Controls"
        xmlns:vms="clr-namespace:Sayra.UI.Notifications.ViewModels"
        mc:Ignorable="d"
        Title="تاریخچه اعلان‌ها" Height="600" Width="480"
        Background="#101014" WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Window Title and Actions Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="تاریخچه اعلان‌ها" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Command="{Binding MarkAllAsReadCommand}" Content="خوانده شده همه" Background="Transparent" Foreground="#F5FF00" BorderThickness="0" Margin="0,0,16,0" Cursor="Hand" VerticalAlignment="Center"/>
                <Button Command="{Binding ClearAllCommand}" Content="پاک کردن همه" Background="Transparent" Foreground="#F46B6B" BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Filter and Search Row -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search TextBox -->
            <TextBox Grid.Column="0" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Background="#1E1E24" Foreground="White" BorderBrush="#2F2F36" BorderThickness="1.5" Height="36" Padding="10,0" VerticalContentAlignment="Center" Margin="0,0,10,0">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!-- Priority Filter ComboBox -->
            <ComboBox Grid.Column="1" ItemsSource="{Binding Priorities}" SelectedItem="{Binding SelectedPriorityFilter}" Width="100" Height="36" VerticalContentAlignment="Center" Background="#1E1E24" Foreground="White" Margin="0,0,10,0"/>

            <!-- Category Filter ComboBox -->
            <ComboBox Grid.Column="2" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategoryFilter}" Width="100" Height="36" VerticalContentAlignment="Center" Background="#1E1E24" Foreground="White"/>
        </Grid>

        <!-- Scrollable Notifications List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Focusable="False">
            <ItemsControl ItemsSource="{Binding Notifications}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vms:NotificationCardViewModel}">
                        <controls:NotificationCard Margin="0,0,0,12"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <TextBlock Grid.Row="3" Text="تمامی اعلان‌ها رمزگذاری شده و ایمن هستند." Foreground="#9A9A9A" FontSize="11" HorizontalAlignment="Center" Margin="0,16,0,0"/>
    </Grid>
</Window>
