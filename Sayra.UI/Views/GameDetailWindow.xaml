<Window x:Class="Sayra.UI.Views.GameDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:Sayra.UI.Controls"
        xmlns:suis="clr-namespace:Sayra.UI.Services"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        xmlns:vm="clr-namespace:Sayra.UI.ViewModels"
        mc:Ignorable="d"
        Title="Game Details"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        FlowDirection="LeftToRight">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/GameDetailTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- Fade-in animation for Window content -->
            <Storyboard x:Key="WindowFadeInStoryboard">
                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6" />
            </Storyboard>

            <!-- Glassmorphic Action Button Style -->
            <Style x:Key="GlassDetailButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource GameDetail.Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Height" Value="50"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12">
                                <Border.Effect>
                                    <DropShadowEffect x:Name="GlowEffect" Color="{DynamicResource App.Accent}" BlurRadius="10" ShadowDepth="0" Opacity="0"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource GameDetail.Button.HoverBg}"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource GameDetail.Button.HoverBorder}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource GameDetail.Button.PressedBg}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Premium End Session Button Style -->
            <Style x:Key="EndSessionButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Height" Value="50"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12">
                                <Border.Effect>
                                    <DropShadowEffect x:Name="GlowEffect" Color="{DynamicResource App.Danger}" BlurRadius="10" ShadowDepth="0" Opacity="0"/>
                                </Border.Effect>
                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Specified End Session Button Icon with stroke style, 1.5 thickness, and transparent fill -->
                                    <Path Grid.Column="0"
                                          Data="{DynamicResource EndSessionIconGeometry}"
                                          Stroke="{DynamicResource DangerBrush}"
                                          StrokeThickness="1.5"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          Fill="Transparent"
                                          Width="16" Height="16"
                                          Stretch="Uniform"
                                          VerticalAlignment="Center"/>

                                    <!-- Text with Peyda Bold, Numbers/Buttons -->
                                    <TextBlock Grid.Column="2"
                                               Text="پایان دادن به استفاده"
                                               Foreground="{DynamicResource DangerBrush}"
                                               FontFamily="{StaticResource PeydaBold}"
                                               FontSize="15"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource GameDetail.Button.DangerHoverBg}"/>
                                    <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="1.5"/>
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="GlowEffect"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="0.6" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetName="GlowEffect"
                                                                 Storyboard.TargetProperty="BlurRadius"
                                                                 To="15" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="GlowEffect"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="0.0" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource GameDetail.Button.DangerPressedBg}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowFadeInStoryboard}" />
        </EventTrigger>
    </Window.Triggers>

    <Grid x:Name="RootGrid" Background="{DynamicResource GameDetail.Background}">
        <!-- 1. Atmospheric heavily blurred game backdrop -->
        <Grid IsHitTestVisible="False">
            <Image x:Name="DetailBackgroundImage" Source="{Binding BackgroundImageSource}" Stretch="UniformToFill" Opacity="0.18">
                <Image.Effect>
                    <BlurEffect Radius="45" RenderingBias="Quality" />
                </Image.Effect>
            </Image>
            <Border Background="{DynamicResource GameDetail.HeroOverlay}"/>
        </Grid>

        <!-- Content Row -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Top Bar Row -->
                <RowDefinition Height="*" />    <!-- Content Row -->
            </Grid.RowDefinitions>

            <!-- EXACT SAME HEADER: Top bar reusable component -->
            <c:TopBar Grid.Row="0" />

            <!-- CONTENT BODY (تقارن ۱۰۰٪ به صورت متقارن و ستونی) -->
            <Grid Grid.Row="1" FlowDirection="LeftToRight" Margin="75,30,75,60">
                <Grid.RowDefinitions>
                    <!-- ردیف دکمه بازگشت و نان‌ریزه -->
                    <RowDefinition Height="Auto" />
                    <!-- فاصله‌گذار -->
                    <RowDefinition Height="35" />
                    <!-- بدنه اصلی متقارن -->
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- هدر: دکمه بازگشت و نان‌ریزه -->
                <Button Grid.Row="0" Command="{Binding BackCommand}" Style="{DynamicResource GlassDetailButtonStyle}" HorizontalAlignment="Left" Width="160">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Path Data="M10 19l-7-7m0 0l7-7m-7 7h18" Stroke="{DynamicResource TextPrimaryBrush}" StrokeThickness="2" Width="16" Height="16" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="بازگشت" FontFamily="{StaticResource PeydaMedium}" FontSize="15" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </StackPanel>
                </Button>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" FlowDirection="RightToLeft">
                    <TextBlock Text="داشبورد / بازی ها / " FontFamily="{StaticResource PeydaMedium}" FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="BreadcrumbGameTitle" Text="{Binding Game.Title}" FontFamily="{StaticResource PeydaBold}" FontSize="16" Foreground="{DynamicResource GameDetail.Highlight}"/>
                </StackPanel>

                <!-- گرید چیدمان اصلی متقارن (کارت‌ها و پنل‌ها به صورت متقارن روی یک ردیف و ستون چیده می‌شوند) -->
                <Grid Grid.Row="2" VerticalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <!-- ستون چپ عریض برای هیرو بنر و جزئیات بازی -->
                        <ColumnDefinition Width="Auto" />
                        <!-- ستون میانی فاصله‌گذار داینامیک -->
                        <ColumnDefinition Width="*" />
                        <!-- ستون راست برای اطلاعات نشست و مشخصات سخت‌افزاری سیستم -->
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <!-- ردیف بالا: هیرو ایمیج و اطلاعات نشست سیستم -->
                        <RowDefinition Height="Auto" />
                        <!-- فاصله‌گذار ردیفی متقارن بدون استفاده از مارجین هک شده -->
                        <RowDefinition Height="20" />
                        <!-- ردیف پایین: اطلاعات تفصیلی بازی به همراه لوگو و پنل سخت‌افزاری سیستم -->
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- ========================================== -->
                    <!-- بخش بالایی (TOP SECTION) -->
                    <!-- ========================================== -->

                    <!-- ستون چپ: کارت اطلاعات تفصیلی بازی به همراه لوگو و مشخصات متنی (منتقل شده به بالا) -->
                    <c:GameInfoCard Grid.Row="0" Grid.Column="0" Width="1296" Height="406" CornerRadius="20" Padding="30" VerticalAlignment="Stretch"
                                    Background="{DynamicResource GameDetail.CardBackground}"
                                    BorderBrush="{DynamicResource GameDetail.Border}"
                                    BorderThickness="1">
                        <Grid FlowDirection="RightToLeft">
                            <Grid.ColumnDefinitions>
                                <!-- بخش راست کارت (RTL Col 0): متون و دکمه شروع بازی -->
                                <ColumnDefinition Width="*" />
                                <!-- فاصله‌گذار ستونی -->
                                <ColumnDefinition Width="30" />
                                <!-- بخش چپ کارت (RTL Col 2): لوگوی تفصیلی بازی -->
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- چپ: لوگوی بازی (بازیابی لوگوی حذف شده) -->
                            <Border Grid.Column="2" VerticalAlignment="Bottom" Width="326" Height="326" Margin="0,10,0,10" Background="{DynamicResource GameDetail.BadgeBackground}" CornerRadius="16" BorderBrush="{DynamicResource GameDetail.Border}" BorderThickness="1" ClipToBounds="True">
                                <Image x:Name="DetailLogoImage" Source="{Binding LogoImageSource}" Stretch="Uniform" Margin="10" RenderOptions.BitmapScalingMode="HighQuality">
                                    <Image.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="1" Opacity="0.5"/>
                                    </Image.Effect>
                                </Image>
                            </Border>

                            <!-- راست: مشخصات متنی بازی (شامل عنوان، توضیحات، سازنده و لانچر) -->
                            <Grid Grid.Column="0" VerticalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" /> <!-- عنوان و ژانر بازی -->
                                    <RowDefinition Height="*" />    <!-- توضیحات بازی -->
                                    <RowDefinition Height="Auto" /> <!-- مدال‌های متادیتا -->
                                    <RowDefinition Height="Auto" /> <!-- وضعیت آماده به بازی / در حال بازی -->
                                </Grid.RowDefinitions>

                                <!-- عنوان و ژانر -->
                                <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,0,10">
                                    <TextBlock x:Name="DetailTitle" Text="{Binding Game.Title}" Style="{DynamicResource GameHeroTitleStyle}" />
                                    <TextBlock x:Name="DetailGenre" Text="{Binding Game.Genre}" Style="{DynamicResource GameHeroGenreStyle}" />
                                </StackPanel>

                                <!-- توضیحات بازی -->
                                <TextBlock x:Name="DetailDescription" Grid.Row="1" Text="{Binding Game.Description}" Style="{DynamicResource GameHeroDescriptionStyle}" />

                                <!-- متادیتاها با استایل سنترالیزه جدید و اولویت تصحیح‌شده WPF -->
                                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" FlowDirection="RightToLeft" Margin="0,0,0,20">
                                    <c:GameBadge x:Name="DeveloperBadge" Text="{Binding Game.Developer}" Style="{DynamicResource CategoryBadgeStyle}" HasLabel="True" Label="سازنده: " Margin="0,0,10,0"/>
                                    <c:GameBadge x:Name="ReleaseYearBadge" Text="{Binding Game.ReleaseYear}" Style="{DynamicResource CategoryBadgeStyle}" HasLabel="True" Label="سال انتشار: " Margin="0,0,10,0"/>
                                    <c:GameBadge x:Name="LauncherBadge" Text="{Binding Game.Launcher}" Style="{DynamicResource GenreBadgeStyle}" HasLabel="True" Label="لانچر: " TextForeground="{DynamicResource GameDetail.Highlight}"/>
                                </StackPanel>

                                <!-- دکمه بزرگ آماده به بازی / درحال بازی -->
                                <Button Grid.Row="3" x:Name="PlayButton" Command="{Binding PlayGameCommand}" Style="{DynamicResource PrimaryActionButtonStyle}" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" HorizontalAlignment="Right" Padding="0" Cursor="Hand" Margin="0,0,0,10">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Border x:Name="StatusBadgeBorder" Width="288" Height="50" BorderBrush="{Binding StatusBorderBrush}" Background="{Binding StatusBackgroundBrush}" CornerRadius="10" BorderThickness="1">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Ellipse x:Name="StatusBadgeDot" Width="8" Height="8" Fill="{Binding StatusDotBrush}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <TextBlock x:Name="DetailStatus" Text="{Binding StatusText}" Foreground="{Binding StatusForegroundBrush}" FontFamily="{StaticResource PeydaMedium}" FontSize="15" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </Button>
                            </Grid>
                        </Grid>
                    </c:GameInfoCard>

                    <!-- ستون راست: کارت اطلاعات نشست سیستم (هم‌تراز با ردیف هیرو بنر بازی) -->
                    <c:GameInfoCard Grid.Row="0" Grid.Column="2" Width="454" Height="406" CornerRadius="20" Padding="25"
                                    Background="{DynamicResource GameDetail.CardBackground}"
                                    BorderBrush="{DynamicResource GameDetail.Border}"
                                    BorderThickness="1">
                        <c:GameInfoCard.DataContext>
                            <vm:SessionHeroViewModel />
                        </c:GameInfoCard.DataContext>
                        <c:GameInfoCard.Effect>
                            <DropShadowEffect Color="{DynamicResource App.Accent}" BlurRadius="12" ShadowDepth="0" Opacity="0.1" />
                        </c:GameInfoCard.Effect>

                        <Grid FlowDirection="RightToLeft">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock x:Name="DetailPcNameText" Text="{Binding PcName}" FontFamily="{StaticResource PeydaBold}" FontSize="24" Foreground="{DynamicResource GameDetail.Highlight}" HorizontalAlignment="Left" VerticalAlignment="Center" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Path Data="M12 6v6h4.5M21 12a9 9 0 11-18 0 9 9 0 0118 0z" Stroke="{DynamicResource GameDetail.Highlight}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Fill="Transparent" Width="22" Height="22" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="session information" FontFamily="{StaticResource PeydaBold}" FontSize="20" Foreground="{DynamicResource GameDetail.Highlight}" VerticalAlignment="Center" />
                                </StackPanel>
                            </Grid>

                            <StackPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,0,15">
                                <TextBlock Text="زمان استفاده شده" FontFamily="{StaticResource PeydaMedium}" FontSize="18" Foreground="{DynamicResource GameDetail.Highlight}" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding SessionTime}" FontFamily="{StaticResource BlackOpsOne}" FontSize="54" Foreground="{DynamicResource GameDetail.Highlight}" HorizontalAlignment="Right" Margin="0,4,0,0">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="{DynamicResource App.Primary}" BlurRadius="15" ShadowDepth="0" Opacity="0.4" />
                                    </TextBlock.Effect>
                                </TextBlock>
                            </StackPanel>

                            <Border Grid.Row="2" Height="1.5" Background="{DynamicResource GameDetail.Highlight}" Margin="0,0,0,15" />

                            <Grid Grid.Row="3" Margin="0,0,0,15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Path Data="{DynamicResource HourlyRateIconGeometry}" Stroke="{DynamicResource GameDetail.Highlight}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Fill="Transparent" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0">
                                            <Path.Effect>
                                                <DropShadowEffect Color="{DynamicResource App.Primary}" BlurRadius="6" ShadowDepth="0" Opacity="0.3"/>
                                            </Path.Effect>
                                        </Path>
                                        <TextBlock Text="نرخ ساعتی" FontFamily="{StaticResource PeydaMedium}" FontSize="19" Foreground="{DynamicResource GameDetail.Highlight}" VerticalAlignment="Center" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding HourlyRate}" FontFamily="{StaticResource PeydaBold}" FontSize="20" Foreground="{DynamicResource GameDetail.Highlight}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                </Grid>

                                <Grid Grid.Row="1" Margin="0,0,0,10">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Path Data="{DynamicResource CurrentCostIconGeometry}" Stroke="{DynamicResource GameDetail.Highlight}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Fill="Transparent" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0">
                                            <Path.Effect>
                                                <DropShadowEffect Color="{DynamicResource App.Primary}" BlurRadius="6" ShadowDepth="0" Opacity="0.3"/>
                                            </Path.Effect>
                                        </Path>
                                        <TextBlock Text="هزینه تا کنون" FontFamily="{StaticResource PeydaMedium}" FontSize="19" Foreground="{DynamicResource GameDetail.Highlight}" VerticalAlignment="Center" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding CurrentCost}" FontFamily="{StaticResource PeydaBold}" FontSize="20" Foreground="{DynamicResource GameDetail.Highlight}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                </Grid>

                                <Grid Grid.Row="2" Margin="0,0,0,10">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Path Data="{DynamicResource StartTimeIconGeometry}" Stroke="{DynamicResource GameDetail.Highlight}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Fill="Transparent" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0">
                                            <Path.Effect>
                                                <DropShadowEffect Color="{DynamicResource App.Primary}" BlurRadius="6" ShadowDepth="0" Opacity="0.3"/>
                                            </Path.Effect>
                                        </Path>
                                        <TextBlock Text="زمان شروع" FontFamily="{StaticResource PeydaMedium}" FontSize="19" Foreground="{DynamicResource GameDetail.Highlight}" VerticalAlignment="Center" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding StartTime}" FontFamily="{StaticResource PeydaBold}" FontSize="20" Foreground="{DynamicResource GameDetail.Highlight}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                </Grid>
                            </Grid>

                            <Button Grid.Row="4" Style="{DynamicResource EndSessionButtonStyle}" Command="{Binding EndSessionCommand}" Width="240" Height="48" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                        </Grid>
                    </c:GameInfoCard>

                    <!-- ========================================== -->
                    <!-- بخش پایینی (BOTTOM SECTION) -->
                    <!-- ========================================== -->

                    <!-- ستون چپ: لوگوی بزرگ سایرا (منتقل شده به بخش پایینی خالی) -->
                    <Grid Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <svgc:SvgViewbox Source="/Assets/sayra.svg" Width="1100" Height="344" Opacity="0.25" IsHitTestVisible="False" />
                    </Grid>

                    <!-- ستون راست: پنل اطلاعات تفصیلی سخت‌افزار سیستم (هم‌تراز با ردیف پایینی) -->
                    <StackPanel Grid.Row="2" Grid.Column="2" VerticalAlignment="Top" Width="454">
                        <c:HardwarePanel x:Name="Hardware" Width="454" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>