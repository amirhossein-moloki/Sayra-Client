<UserControl x:Class="Sayra.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:c="clr-namespace:Sayra.UI.Controls"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             FlowDirection="RightToLeft">
    <Grid Background="{StaticResource BackgroundBrush}">
        <!-- Background Image Layer -->
        <Image Source="/Assets/photo_2026-07-08_22-07-56.jpg" Stretch="UniformToFill" Opacity="0.4">
            <Image.Effect>
                <BlurEffect Radius="10"/>
            </Image.Effect>
        </Image>

        <!-- Dark Overlay -->
        <Border Background="#CC0D0D12"/>

        <Grid Margin="60,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0">
                    <svgc:SvgViewbox Source="/Assets/sayra.svg" Width="140" Height="40" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding PcName}" Style="{StaticResource HeadingText}" FontSize="24" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <c:StatusBadge VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentTime}" Style="{StaticResource HeadingText}" FontSize="18" Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding CurrentDate}" Style="{StaticResource SecondaryText}" FontSize="16" Margin="0,0,25,0" VerticalAlignment="Center"/>
                    <Button Width="40" Height="40" Style="{StaticResource PlayButton}" Background="Transparent">
                         <Path Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8z"
                               Fill="White" Width="20" Height="20" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Hero Session Area -->
            <Grid Grid.Row="1" Margin="0,40,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Left">
                    <c:SessionTimer/>
                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <c:PriceCard Title="هزینه تاکنون" Value="{Binding TotalCost}" Margin="0,0,20,0"/>
                        <c:PriceCard Title="نرخ ساعتی" Value="{Binding HourlyRate}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="2" Margin="0,60,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <!-- Game Library -->
                <StackPanel Grid.Column="0">
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="لیست بازی‌ها" Style="{StaticResource HeadingText}" FontSize="24" VerticalAlignment="Center"/>
                        <Border Grid.Column="2" Background="#1AFFFFFF" CornerRadius="8" Padding="15,8" Width="300">
                             <TextBlock Text="جستجو..." Style="{StaticResource SecondaryText}" Opacity="0.5"/>
                        </Border>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding Games}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="5"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <c:GameCard/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- System Panel -->
                <StackPanel Grid.Column="1" Margin="40,0,0,0">
                    <TextBlock Text="مشخصات سیستم" Style="{StaticResource HeadingText}" FontSize="20" Margin="0,0,0,20"/>
                    <ItemsControl ItemsSource="{Binding SystemSpecs}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <c:SystemInfoCard/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>

            <!-- Bottom Action -->
            <Border Grid.Row="3" BorderBrush="#1AFFFFFF" BorderThickness="0,1,0,0">
                <Button Content="پایان دادن به استفاده" Style="{StaticResource ShutdownButton}"
                        HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16"/>
            </Border>

        </Grid>
    </Grid>
</UserControl>
