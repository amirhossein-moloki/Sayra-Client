<Window x:Class="Sayra.Client.UI.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Sayra.Client.UI.Controls" xmlns:v="clr-namespace:Sayra.Client.UI.Views" xmlns:svgc="http://sharpvectors.codeplex.com/svgc/" xmlns:suic="clr-namespace:Sayra.UI.Controls;assembly=Sayra.UI" xmlns:suis="clr-namespace:Sayra.UI.Services;assembly=Sayra.UI" Title="Sayra Gaming Center" Width="1920" Height="1080" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="CanResize" WindowState="Maximized" UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
<Grid Background="{StaticResource AppBackgroundBrush}">
    <Grid.OpacityMask><SolidColorBrush Color="White"/></Grid.OpacityMask>

    <Image Source="pack://application:,,,/Assets/photo_2026-07-08_22-07-56.jpg" Stretch="UniformToFill" Opacity=".44" RenderOptions.BitmapScalingMode="HighQuality">
        <Image.RenderTransform><TranslateTransform Y="-18"/></Image.RenderTransform>
        <Image.Effect><BlurEffect Radius="1.6"/></Image.Effect>
    </Image>
    <Rectangle Fill="#BB06060A"/>
    <Rectangle>
        <Rectangle.Fill>
            <RadialGradientBrush Center=".30,.25" GradientOrigin=".30,.25" RadiusX=".72" RadiusY=".55">
                <GradientStop Color="#301E2230" Offset="0"/>
                <GradientStop Color="#DD08080C" Offset="1"/>
            </RadialGradientBrush>
        </Rectangle.Fill>
    </Rectangle>

    <Canvas IsHitTestVisible="False" ClipToBounds="True">
        <TextBlock Canvas.Left="845" Canvas.Top="116" Text="SAYRA" FontSize="216" FontFamily="{StaticResource PeydaBlack}" Foreground="White" Opacity=".045"/>
        <Rectangle Canvas.Left="1260" Canvas.Top="142" Width="420" Height="178" Opacity=".17">
            <Rectangle.Fill>
                <DrawingBrush TileMode="Tile" Viewport="0,0,24,24" ViewportUnits="Absolute" Stretch="None">
                    <DrawingBrush.Drawing>
                        <GeometryDrawing Brush="{StaticResource PrimaryAccentBrush}" Geometry="M 2,2 A 2,2 0 1 1 1.99,2"/>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Rectangle.Fill>
        </Rectangle>
        <Path Stroke="{StaticResource PrimaryAccentBrush}" StrokeThickness="1" Opacity=".18" Data="M74,246 L640,86 M1198,122 L1778,342 M260,900 L1020,760"/>
    </Canvas>

    <Grid Margin="65,0,65,34">
        <Grid.RowDefinitions><RowDefinition Height="72"/><RowDefinition Height="260"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="72">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <svgc:SvgViewbox Source="pack://application:,,,/Assets/sayra.svg" Width="140" Height="40" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="PC-08" FontFamily="{StaticResource PeydaBold}" FontSize="22" Margin="0,0,16,0" VerticalAlignment="Center"/>
                <Border CornerRadius="14" BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1" Padding="12,4" Background="#06000000" Height="29">
                    <StackPanel Orientation="Horizontal"><Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}" Margin="0,0,8,0" VerticalAlignment="Center"><Ellipse.Effect><DropShadowEffect Color="#22C55E" BlurRadius="12" ShadowDepth="0" Opacity="1"/></Ellipse.Effect></Ellipse><TextBlock Text="Connected" Foreground="{StaticResource SuccessBrush}" FontSize="12" FontFamily="{StaticResource PeydaBold}" VerticalAlignment="Center"/></StackPanel>
                </Border>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <StackPanel Margin="0,0,18,0"><TextBlock Text="{Binding ClockText}" FontFamily="{StaticResource PeydaBold}" FontSize="22" HorizontalAlignment="Right"/><TextBlock Text="{Binding DateText}" FontFamily="{StaticResource PeydaLight}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" HorizontalAlignment="Right"/></StackPanel>
                <Button Style="{StaticResource IconButton}" Command="{Binding PowerCommand}" Width="36" Height="36" Content="⏻" FontSize="17"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" CornerRadius="28" Background="#59181820" BorderBrush="#10FFFFFF" BorderThickness="1" Padding="39,20,36,20" ClipToBounds="True">
            <Border.Effect><DropShadowEffect Color="#000000" BlurRadius="38" ShadowDepth="14" Opacity=".42"/></Border.Effect>
            <Grid>
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                    <controls:SessionTimer TimeText="{Binding RemainingTimeText}"/>
                    <StackPanel Orientation="Horizontal" Margin="1,12,0,0">
                        <controls:PriceCard Title="هزینه تاکنون" Value="110,000 تومان" Icon="◈" Margin="0,0,18,0"/>
                        <controls:PriceCard Title="نرخ ساعتی" Value="120,000 تومان" Icon="⌁"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock Text="SAYRA" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="132" FontFamily="{StaticResource PeydaBlack}" Foreground="White" Opacity=".015"/>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="PC-08" FontFamily="{StaticResource BlackOpsOne}" FontSize="88" Foreground="{StaticResource PrimaryAccentBrush}">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#F5FF00" BlurRadius="20" ShadowDepth="0" Opacity="0.45"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,30,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="36"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions>
            <Grid FlowDirection="RightToLeft"><Grid.RowDefinitions><RowDefinition Height="52"/><RowDefinition Height="*"/></Grid.RowDefinitions><DockPanel><TextBlock Text="🎮" FontSize="23" DockPanel.Dock="Right"/><TextBlock Text="بخش بازی های" FontFamily="{StaticResource PeydaBold}" FontSize="26" DockPanel.Dock="Right" Margin="10,0,0,0"/><Border Width="280" Height="40" Background="#D9181820" BorderBrush="#18FFFFFF" BorderThickness="1" CornerRadius="20" HorizontalAlignment="Left" DockPanel.Dock="Left" Padding="14,0"><DockPanel LastChildFill="True"><TextBlock Text="⌕" FontSize="16" Foreground="#9CA3AF" VerticalAlignment="Center" DockPanel.Dock="Right"/><TextBlock Text="نام بازی را جستجو کنید..." FontFamily="{StaticResource PeydaLight}" FontSize="13" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="8,0"/></DockPanel></Border></DockPanel><ItemsControl Grid.Row="1" ItemsSource="{Binding Games}" FlowDirection="LeftToRight"><ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="5" Rows="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><controls:GameCard Margin="0,0,19,22"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></Grid>
            <StackPanel Grid.Column="2" HorizontalAlignment="Right"><controls:SystemInfoCard/><Button Content="پایان دادن به استفاده" Command="{Binding ShutdownCommand}" Style="{StaticResource DangerButton}" Width="300" Margin="0,22,0,0" FlowDirection="RightToLeft"/></StackPanel>
        </Grid>
    </Grid>
    <v:WarningOverlay Visibility="{Binding Warnings.IsWarningVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

    <!-- Floating Horizontal Notification Card -->
    <Grid VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,40" Panel.ZIndex="1000" IsHitTestVisible="True"
          DataContext="{Binding Source={x:Static suis:NotificationService.Instance}}"
          Visibility="{Binding IsNotificationVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
        <suic:NotificationCard DataContext="{Binding CurrentNotification}" />
    </Grid>
</Grid>
</Window>
