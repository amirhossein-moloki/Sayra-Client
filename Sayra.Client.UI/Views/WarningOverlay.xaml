<UserControl x:Class="Sayra.Client.UI.Views.WarningOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource Theme.Brushes.Surface.Dialog}">
        <Border Background="{DynamicResource Theme.Brushes.Surface.Dialog}" CornerRadius="10" Padding="40"
                VerticalAlignment="Center" HorizontalAlignment="Center"
                MaxWidth="600" BorderThickness="2">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brushes.Status.Warning}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Warnings.IsCritical}" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brushes.Status.Error}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="⚠️" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="{Binding Warnings.WarningMessage}"
                           FontSize="24" FontWeight="Bold" Foreground="White"
                           TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,30"/>

                <Button Content="Dismiss" Width="120" Height="40"
                        Command="{Binding Warnings.HideWarningCommand}"
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
