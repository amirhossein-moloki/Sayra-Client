<UserControl x:Class="Sayra.Client.UI.Views.LauncherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Sayra.Client.UI.Views"
             xmlns:vm="clr-namespace:Sayra.Client.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1000">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header / Search -->
            <RowDefinition Height="*"/>    <!-- App Grid -->
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <Border Grid.Row="0" Background="#252525" CornerRadius="20" Padding="20,10" Margin="0,0,0,30" HorizontalAlignment="Left" Width="400">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#888"/>
                <TextBox Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Background="Transparent" BorderThickness="0" Foreground="White"
                         FontSize="16" VerticalAlignment="Center" CaretBrush="White"/>
            </Grid>
        </Border>

        <!-- App Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredApps}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="180" Height="240" Margin="0,0,20,20" Background="#1E1E1E" CornerRadius="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="150"/> <!-- Icon/Image Placeholder -->
                                    <RowDefinition Height="*"/>    <!-- Name / Launch -->
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Background="#2A2A2A" CornerRadius="10,10,0,0">
                                    <TextBlock Text="{Binding Name[0]}" Foreground="Cyan" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Row="1" Margin="10">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="White" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Category}" Foreground="#888" FontSize="11" Margin="0,2,0,5"/>

                                    <Button Content="LAUNCH" Height="30" Background="Cyan" Foreground="Black" FontWeight="Bold"
                                            Command="{Binding DataContext.LaunchAppCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="15"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
