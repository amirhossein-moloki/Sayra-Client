<UserControl x:Class="Sayra.Client.UI.Views.BillingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="300">
    <Border Background="#252525" CornerRadius="5" Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="RATE/HR" FontSize="10" Foreground="#888"/>
                <TextBlock Text="{Binding HourlyRate}" FontWeight="Bold" Foreground="White"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="CURRENT" FontSize="10" Foreground="#888"/>
                <TextBlock Text="{Binding CurrentCost}" FontWeight="Bold" Foreground="#4CAF50"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="ESTIMATED" FontSize="10" Foreground="#888"/>
                <TextBlock Text="{Binding EstimatedTotal}" FontWeight="Bold" Foreground="#2196F3"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
