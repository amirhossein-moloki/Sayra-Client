<UserControl x:Class="Sayra.Client.UI.Views.BillingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="300">
    <Border Background="{DynamicResource Theme.Brushes.Background.Secondary}" CornerRadius="5" Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="RATE/HR" FontSize="10" Foreground="{DynamicResource Theme.Brushes.Text.Muted}"/>
                <TextBlock Text="{Binding HourlyRate}" FontWeight="Bold" Foreground="White"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="CURRENT" FontSize="10" Foreground="{DynamicResource Theme.Brushes.Text.Muted}"/>
                <TextBlock Text="{Binding CurrentCost}" FontWeight="Bold" Foreground="{DynamicResource Theme.Brushes.Status.Success}"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="ESTIMATED" FontSize="10" Foreground="{DynamicResource Theme.Brushes.Text.Muted}"/>
                <TextBlock Text="{Binding EstimatedTotal}" FontWeight="Bold" Foreground="{DynamicResource Theme.Brushes.Secondary}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
