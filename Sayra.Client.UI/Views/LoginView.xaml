<UserControl x:Class="Sayra.Client.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Sayra.Client.UI.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="#1A1A1A" CornerRadius="10"
                Width="350" Height="450" VerticalAlignment="Center" HorizontalAlignment="Center"
                BorderBrush="#333" BorderThickness="1">
            <StackPanel Margin="30">
                <TextBlock Text="SAYRA" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,40" Foreground="Cyan"/>

                <TextBlock Text="ENTER PIN" FontSize="14" Foreground="#888" Margin="0,0,0,10"/>
                <PasswordBox x:Name="PinBox" Height="45" Background="#222" Foreground="White" BorderBrush="#444" FontSize="24"
                             HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                             PasswordChanged="PinBox_PasswordChanged"/>

                <Button Content="LOGIN" Height="45" Margin="0,20,0,0"
                        Background="Cyan" Foreground="Black" FontWeight="Bold" FontSize="16"
                        Command="{Binding LoginCommand}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextAlignment="Center" TextWrapping="Wrap"/>

                <ProgressBar IsIndeterminate="True" Height="4" Margin="0,20,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
