<UserControl x:Class="Sayra.Client.UI.Views.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Sayra.Client.UI.Views"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="{Binding RemainingTimeStr}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="Cyan"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLowTime}" Value="True">
                                <Setter Property="Foreground" Value="OrangeRed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Low Time Warning Icon -->
            <TextBlock Text="⚠️" Margin="5,0,0,0" VerticalAlignment="Center"
                       Visibility="{Binding IsLowTime, Converter={StaticResource BooleanToVisibilityConverter}}"
                       ToolTip="Low balance! Please top up."/>
        </StackPanel>
    </Grid>
</UserControl>
