<UserControl x:Class="Sayra.Client.UI.Controls.PriceCard" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Width="220" Height="70" FlowDirection="RightToLeft">
    <Border Background="{DynamicResource Theme.Brushes.Surface.Card}" BorderBrush="{DynamicResource Theme.Brushes.White.Opacity10}" BorderThickness="1" CornerRadius="18" Padding="16,10"><Border.Effect><DropShadowEffect Color="#000000" BlurRadius="22" ShadowDepth="8" Opacity=".35"/></Border.Effect>
        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="46"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Width="38" Height="38" CornerRadius="12" Background="{DynamicResource Theme.Brushes.Primary.Opacity13}" HorizontalAlignment="Center" VerticalAlignment="Center"><TextBlock Text="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="18" Foreground="{StaticResource PrimaryAccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0"><TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="12" Foreground="{DynamicResource Theme.Brushes.Primary}" FontFamily="{StaticResource PeydaLight}"/><TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="22" Foreground="White" FontFamily="{StaticResource PeydaBold}" Margin="0,3,0,0"/></StackPanel>
        </Grid>
    </Border>
</UserControl>
